{
  "name": "postman-scripts",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "./dist/index.js",
  "scripts": {
    "build": "rm -fr dist && npx tsc && npx pkgroll && cp dist/index.js ./postman-scripts.js"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
